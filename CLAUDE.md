# CLAUDE.md

## 核心协作原则

1. 固定称呼规则：每次回复前先称呼用户为 `fourone`。
2. 决策前置确认：遇到不确定的设计决策，必须先向用户确认，不得擅自实现。
3. 抑制兼容性代码：非用户显式要求，不得为了兼容旧逻辑生成额外分支或冗余路径。

## 项目背景

本项目用于医疗器械 DHF/DMR 文档一致性审查。当前重点是基于已转换的 PT9L Markdown 文档，使用 `m-agent` 中的多智能体 LangGraph 流程完成全量审查、机械候选复审、证据核验、Challenge 裁决和报告生成。


## 工作约束

- 优先保持 `m-agent/` 作为最终版本代码和技术文档的唯一维护入口。
- 修改审查逻辑前，先确认是否影响既有输出目录和历史结果。
- 不要把旧实验目录重新引入主流程，除非用户明确要求对比或恢复。

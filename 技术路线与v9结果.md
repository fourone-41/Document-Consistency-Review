# PT9L DHF/DMR 多智能体一致性审查：技术路线与 v9 结果汇报

> 当前主线：`m-agent/`
> 当前推荐结果：`magent-output/pt9l_full_audit_output_langgraph_v9/`

## 1. 一句话结论

本项目当前已经从早期的固定流程/规则匹配方案，演进为一套基于 **LangGraph + 多智能体 + GPT55 大模型 + 证据索引工具** 的 DHF/DMR 文档一致性审查系统。系统以 Orchestrator 动态派发任务为核心，由多个领域 Agent 对 PT9L 的 470 份 Markdown 文档进行审查，再经过 Evidence Gate、Challenge、Context Recovery、Dedup/Grouping 和 Report Agent 形成可人工审阅的中文 HTML 报告。

v9 结果中，系统共生成 98 条语义 finding，经 Challenge 后保留/修订 90 条，再经 Dedup/Grouping Agent 收束为 61 个问题组。当前最适合人工审阅的交付物是：

```text
magent-output/pt9l_full_audit_output_langgraph_v9/08_reports/grouped-findings-review.html
```

该报告支持按问题组审阅、查看原文证据、人工勾选判断、填写备注，并导出/导入人工审阅结果。

## 2. 当前技术路线

### 2.1 核心思路

当前路线不是“先写死检查项，再按关键词扫文档”的机械方案，而是采用“证据基础设施 + 动态 Orchestrator + 领域 Agent + 人工审阅报告”的路线：

1. 先把 PT9L 的 Markdown 文档构造成可检索的证据底座。
2. 由 Project Scout Agent 建立项目画像和范围边界。
3. 由 Orchestrator 读取 routing policy、项目画像、历史 agent_runs 和候选状态，动态决定下一轮派发哪些 Agent。
4. Candidate Discovery Agent 只提出候选假设，不直接决定问题成立。
5. 领域 Agent 结合 PageIndex、RelationIndex、全文检索和文件读取进行审查。
6. Evidence Gate 检查 finding 是否具备可追溯证据。
7. Challenge Agent 对 finding 做反向质疑，决定 keep、revise、drop 或补证。
8. Context Recovery Agent 对上下文不足的问题进行最多 5 轮补证。
9. Dedup/Grouping Agent 将重复或同根因 finding 收束为问题组。
10. Report Agent 生成中文、可审阅、可导出人工判断的 HTML 报告。

### 2.2 为什么采用这个路线

早期方案倾向于用预设词表或固定模式去抽取候选，这种方式容易出现两个问题：

- 漏检：没有命中词表的型号、文档编号、功能表述可能被跳过。
- 误判：命中关键词不代表问题成立，仍需要上下文和原文证据判断。

当前方案把“候选发现”和“问题确认”分开：

- 工具只负责取证，不直接裁决。
- Candidate Discovery Agent 只负责提出可疑点。
- Orchestrator 负责把任务派给合适的领域 Agent。
- 领域 Agent、Challenge Agent 和 Context Recovery 决定问题是否成立。
- 最终报告以问题组和原文证据为中心，给人工复核留出空间。

这样做的好处是召回率更高，路径也更可解释；缺点是 LLM 调用较多，运行时间和成本会高于纯规则扫描。

## 3. 使用的技术与框架

| 类别 | 技术/框架 | 当前用途 |
|---|---|---|
| 主语言 | Python | 主审查脚本、证据处理、Agent 调度、报告生成。 |
| 流程编排 | LangGraph `StateGraph` | 组织 bootstrap、scout、orchestrator、challenge、context recovery、dedup、report 等节点。 |
| 大模型调用 | OpenAI-compatible SDK | 调用公司内部 GPT55 API；接口形式兼容 OpenAI SDK。 |
| HTTP 客户端 | httpx | 支持 GPT55/OpenAI-compatible 客户端请求配置。 |
| 配置加载 | PyYAML | 每个 Agent 的角色、检索策略、输入 schema、输出 schema 都拆到 YAML。 |
| 环境变量 | python-dotenv | 从 `.env` 加载 API Key、base URL、model 等运行配置。 |
| 数据中间格式 | JSON / JSONL | 保存 run summary、agent_runs、findings、context recovery、grouping results。 |
| 输入格式 | Markdown | PT9L 原始文档已经转换为 Markdown 后进入审查流程。 |
| 报告格式 | 静态 HTML + Markdown | HTML 用于人工审阅；Markdown/JSON 用于归档和二次处理。 |
| 人工审阅状态 | 浏览器 localStorage + JSON 导出/导入 | 页面内勾选和备注可本地保存，也可导出为 JSON 交接。 |

当前主脚本：

```text
m-agent/run_pt9l_full_audit_langgraph.py
```

当前 Agent 配置目录：

```text
m-agent/agent_configs/
```

主要配置文件包括：

```text
project_scout.yaml
orchestrator.yaml
routing.yaml
candidate_discovery.yaml
regulatory.yaml
hardware.yaml
software.yaml
risk_traceability.yaml
vv.yaml
dmr_sop.yaml
challenge.yaml
context_recovery.yaml
dedup_grouping.yaml
report.yaml
report_corrections.yaml
```

## 4. 当前架构说明

### 4.1 证据基础设施

系统先从 Markdown 文档中构建多个证据层：

| 证据层 | 作用 |
|---|---|
| Manifest | 记录输入文档清单、路径、阶段、文档类型等基础信息。 |
| PageIndex | 将文档拆成可定位的段落/页面级证据块。 |
| Parameter Instances | 抽取温度、时间、版本、文档编号等参数实例。 |
| RelationIndex | 建立文档编号、型号、版本、引用关系等结构化关系。 |
| Full-text Search | 支持 Agent 按关键词或文档编号检索证据。 |
| File Reader | 在需要时读取原文上下文。 |

这些工具不是 Agent，也不直接产出“问题成立”的结论；它们是所有 Agent 共享的取证能力。

### 4.2 Orchestrator 动态派发

Orchestrator 是当前方案的核心。它不是固定 workflow 的一个普通节点，而是任务路由中心。

它会读取：

- `routing.yaml` 中的调度策略；
- Project Scout 输出的项目画像；
- 可用 Agent 列表；
- 历史 `agent_runs[]`；
- Candidate Discovery 结果；
- Context Recovery 结果；
- 必查矩阵覆盖状态。

然后动态生成 `route_plan`，决定：

- 是否继续派发；
- 派发给哪些 Agent；
- 每个 Agent 的任务类型；
- 每个任务关注什么审查点；
- 需要哪些证据策略；
- 哪些事项进入 human_review。

v9 中，Orchestrator 共生成 4 个 route plan：前 3 轮继续派发，第 4 轮停止。

### 4.3 Agent 分工

| Agent | 职责 |
|---|---|
| Project Scout Agent | 建立项目画像、型号/功能边界和文档范围判断。 |
| Candidate Discovery Agent | 从全局文档中提出候选假设，不直接判定问题成立。 |
| Regulatory Agent | 审查法规、标准、文档控制、适用性声明等一致性。 |
| Hardware Agent | 审查硬件参数、BOM、图纸、设计输出等一致性。 |
| Software Agent | 审查软件需求、软件设计、版本和软件生命周期相关一致性。 |
| Risk Traceability Agent | 审查风险、危害、风险控制措施和验证关闭证据。 |
| V&V Agent | 审查验证确认计划、报告和需求/风险闭环。 |
| DMR/SOP Agent | 审查 DMR、SOP、BOM、标签、工艺文件等生产侧一致性。 |
| Challenge Agent | 反向质疑 finding，判断 keep、revise、drop 或补证。 |
| Context Recovery Agent | 对证据不足问题执行补证循环。 |
| Dedup/Grouping Agent | 将重复 finding 收束为问题组，并重新判断组级严重等级。 |
| Report Agent | 生成中文、可追溯、可人工审阅的 HTML 报告。 |

### 4.4 关键边界

当前方案明确区分几个边界：

- Orchestrator 只做任务派发，不直接确认或驳回 finding。
- Evidence Tools 只负责取证，不直接生成候选问题。
- Candidate Discovery Agent 只提出候选假设，不直接形成最终结论。
- 只有 Orchestrator 动态派到的 Agent 才真实调用 LLM。
- 一旦派到某个 Agent，就必须真实调用 LLM，不读取旧 Agent 输出缓存。
- Dedup/Grouping 和 Report 是后处理节点，不应替代领域 Agent 的审查职责。

## 5. v9 运行结果总览

v9 是当前建议作为交接基线的结果版本。

运行摘要来源：

```text
magent-output/pt9l_full_audit_output_langgraph_v9/08_reports/run_summary_full_langgraph.json
```

分组摘要来源：

```text
magent-output/pt9l_full_audit_output_langgraph_v9/15_dedup_grouping/grouping_results.json
```

### 5.1 输入与索引规模

| 指标 | 数值 | 说明 |
|---|---:|---|
| 审查模式 | full_langgraph_llm | LangGraph + LLM 全量审查模式。 |
| LLM Provider | GPT55 via OpenAI-compatible SDK | 调用公司内部 GPT55 API。 |
| Manifest 文档数 | 470 | 本轮进入审查的 Markdown 文档总数。 |
| Primary scope 文档数 | 470 | 本轮主审查范围内的文档数。 |
| Auxiliary scope 文档数 | 0 | 本轮未单独设置辅助范围文档。 |
| PageIndex 段落数 | 4,222 | 可供 Agent 检索和引用的段落/页面级证据块。 |
| 参数实例数 | 12,168 | 从文档中抽取的参数、编号、版本等实例。 |
| RelationIndex 节点数 | 4,000 | 文档、型号、编号、参数等关系节点。 |
| RelationIndex 边数 | 189 | 文档间或实体间的结构化关系边。 |
| 证据工具命中数 | 366 | Agent 审查过程中命中的证据工具结果。 |

### 5.2 Orchestrator 派发与 Agent 运行

| 指标 | 数值 |
|---|---:|
| Orchestrator route plan 数 | 4 |
| 继续派发轮次 | 3 |
| 停止派发轮次 | 1 |
| 总 assignment 数 | 13 |
| Agent 真实运行数 | 13 |
| completed 运行数 | 13 |
| candidate_discovery 任务数 | 1 |
| domain_review 任务数 | 6 |
| context_recheck 任务数 | 6 |

各 Agent 运行次数：

| Agent | 运行次数 | 输出 finding 数 |
|---|---:|---:|
| Candidate Discovery Agent | 1 | 11 |
| Hardware Agent | 2 | 11 |
| Risk Traceability Agent | 2 | 14 |
| Software Agent | 1 | 5 |
| DMR/SOP Agent | 2 | 14 |
| Regulatory Agent | 3 | 22 |
| V&V Agent | 2 | 11 |
| 合计 | 13 | 98 |

说明：这里的 98 条为进入 Challenge 前的语义 finding 总量。

### 5.3 Challenge 前后 finding 变化

Challenge 前：

| 严重等级 | 数量 |
|---|---:|
| P0 | 3 |
| P1 | 39 |
| P2 | 47 |
| P3 | 9 |
| 合计 | 98 |

Challenge 后：

| 状态 | 数量 | 说明 |
|---|---:|---|
| keep | 55 | 保留原 finding。 |
| revise | 35 | 修订后保留。 |
| drop | 8 | 经质疑后驳回。 |
| 合计 | 98 | 全部经过 Evidence Gate 与 Challenge。 |

Challenge 后保留/修订的问题共 90 条。所有 98 条 finding 均有 `evidence_status=verified`，说明都已经经过证据引用校验。

Challenge 后保留/修订的 90 条原始 finding 来源：

| 来源 Agent | 数量 |
|---|---:|
| Regulatory Agent | 19 |
| Risk Traceability Agent | 12 |
| DMR/SOP Agent | 12 |
| Hardware Agent | 11 |
| V&V Agent | 11 |
| Project Scout Agent | 10 |
| Candidate Discovery Agent | 10 |
| Software Agent | 5 |
| 合计 | 90 |

保留/修订后的原始 finding 严重等级：

| 严重等级 | 数量 |
|---|---:|
| P0 | 1 |
| P1 | 33 |
| P2 | 33 |
| P3 | 23 |
| 合计 | 90 |

### 5.4 Context Recovery 补证结果

Context Recovery 用于处理需要补充上下文的问题。v9 中执行了 29 个补证任务。

| 指标 | 数值 |
|---|---:|
| Context Recovery 总任务数 | 29 |
| 最大允许轮数 | 5 |
| 实际执行最大轮数 | 5 |
| 最终 confirm | 25 |
| 最终 dismiss | 4 |
| human_review | 0 |

按补证轮数统计：

| 使用轮数 | 任务数 |
|---|---:|
| 1 轮 | 24 |
| 2 轮 | 3 |
| 3 轮 | 1 |
| 5 轮 | 1 |

按补证需求类型统计：

| 缺口类型 | 数量 | 说明 |
|---|---:|---|
| product_identity_context | 14 | 需要补充产品型号、身份、范围边界相关证据。 |
| risk_control_verification_context | 11 | 需要补充风险控制与验证关闭证据。 |
| traceability_context | 3 | 需要补充追溯链条证据。 |
| neighboring_evidence_context | 1 | 需要补充相邻上下文证据。 |

按目标 Agent 统计：

| 目标 Agent | 补证任务数 |
|---|---:|
| DMR/SOP Agent | 8 |
| Risk Traceability Agent | 5 |
| Project Scout Agent | 4 |
| Regulatory Agent | 4 |
| Hardware Agent | 3 |
| Candidate Discovery Agent | 2 |
| V&V Agent | 2 |
| Software Agent | 1 |

这部分说明：系统不是简单地把 prompt 加长后重跑，而是先判断缺什么证据，再按缺口类型重新派发给合适 Agent。

### 5.5 Dedup/Grouping 收束结果

Dedup/Grouping Agent 的目标是把重复、同根因或同整改动作的 finding 收束成问题组，减少人工审阅负担。

| 指标 | 数值 |
|---|---:|
| 输入原始确认 finding 数 | 90 |
| 分组后问题组数 | 61 |
| 被挂载到问题组的原始 finding 数 | 90 |
| 多 finding 合并组数 | 15 |
| 单 finding 问题组数 | 46 |
| 人工修正组数 | 1 |

从 90 条原始确认问题收束到 61 个问题组，减少了 29 个直接审阅单元，收束比例约为 32.2%。如果从最初 98 条语义 finding 计算，到 61 个问题组，审阅单元减少 37 条，收束比例约为 37.8%。

按问题组严重等级统计：

| 组级严重等级 | 问题组数 |
|---|---:|
| P1 | 24 |
| P2 | 21 |
| P3 | 16 |
| 合计 | 61 |

按合并决策统计：

| 合并决策 | 问题组数 | 说明 |
|---|---:|---|
| single_finding | 45 | 单条 finding 独立成组。 |
| merge_related | 10 | 相关 finding 归为同一问题组。 |
| merge_exact_duplicate | 5 | 完全重复或高度重复 finding 合并。 |
| manual_correction_false_premise | 1 | 人工修正错误前提后重新成组。 |

按每组包含的原始 finding 数统计：

| 每组原始 finding 数 | 问题组数 |
|---|---:|
| 1 | 46 |
| 2 | 8 |
| 3 | 4 |
| 4 | 1 |
| 6 | 2 |

### 5.6 人工修正案例：蓝牙问题

v9 中沉淀了一个重要的人工修正组：

```text
MANUAL-BT-001
```

问题背景：

原报告曾出现“PT9L 具备蓝牙功能，但缺少专项危害分析与验证关闭证据”的结论。人工复核后确认：PT9L 实际没有蓝牙功能。设计输入、设计验证计划和设计验证报告均将蓝牙相关项目标记为不适用。

因此，正确结论应从：

```text
PT9L 有蓝牙功能，但缺少蓝牙验证
```

修正为：

```text
风险文件中存在蓝牙/PT3SBT 相关描述，属于范围冲突、模板残留或旧型号残留，需要修订风险文件或补充正式适用性说明。
```

这个案例说明两点：

- 系统需要区分“风险文件中的一句描述”和“受控产品定义中的真实功能”。
- 人工修正不会删除原始 finding，而是保留原始 finding，并在报告中重新解释问题前提和整改方向。

相关沉淀位置：

```text
m-agent/agent_configs/project_scout.yaml
m-agent/agent_configs/report_corrections.yaml
.codex/skills/dhf-dmr-report-writer/SKILL.md
```

## 6. 当前主要交付物

| 类型 | 路径 | 用途 |
|---|---|---|
| 人工审阅主报告 | `magent-output/pt9l_full_audit_output_langgraph_v9/08_reports/grouped-findings-review.html` | 当前最推荐打开给评审人员看的结果。 |
| 分组结果 JSON | `magent-output/pt9l_full_audit_output_langgraph_v9/15_dedup_grouping/grouping_results.json` | Dedup/Grouping 结构化结果。 |
| 分组 finding JSONL | `magent-output/pt9l_full_audit_output_langgraph_v9/07_findings/grouped_findings.jsonl` | 每行一个问题组，便于脚本处理。 |
| 运行摘要 | `magent-output/pt9l_full_audit_output_langgraph_v9/08_reports/run_summary_full_langgraph.json` | v9 统计口径来源。 |
| Agent 运行记录 | `magent-output/pt9l_full_audit_output_langgraph_v9/05_agent_notes/agent_runs.jsonl` | 每次 Orchestrator 派发后的 Agent 运行记录。 |
| Orchestrator 路由计划 | `magent-output/pt9l_full_audit_output_langgraph_v9/10_task_packages/orchestrator_route_plans.json` | 每轮动态派发计划。 |
| Context Recovery 结果 | `magent-output/pt9l_full_audit_output_langgraph_v9/14_context_recovery/context_recovery_results.jsonl` | 补证闭环结果。 |
| 架构可视化 | `最新多智能体DHF-DMR流程架构可视化.html` | 面向讲解和展示的架构图。 |
| 结果目录索引 | `magent-output/all_output.md` | 当前输出目录的简要入口索引。 |

## 7. 当前方案优势

1. **动态调度**：不是固定流程，Orchestrator 可以根据项目画像、候选和历史运行情况派发任务。
2. **召回更好**：不依赖机械词表决定候选，减少“词表没覆盖就漏掉”的问题。
3. **证据可追溯**：finding 必须挂回原文路径、证据引用和上下文。
4. **可反向质疑**：Challenge Agent 会对问题进行保留、修订或驳回。
5. **可补证闭环**：Context Recovery 最多 5 轮补充上下文，不是一轮判断到底。
6. **可降低人工审阅负担**：Dedup/Grouping 将 90 条确认问题收束为 61 个问题组。
7. **可人工纠偏**：蓝牙问题已沉淀为人工修正案例，避免错误前提继续传播。
8. **报告可交接**：HTML 报告支持勾选、备注、localStorage 保存和 JSON 导出/导入。

## 8. 当前限制与风险

1. **结果仍需人工复核**
   v9 报告是候选审查输出，不等于正式质量结论。高风险问题必须由质量/法规/研发人员确认。

2. **运行时间较长**
   系统会真实调用多个 LLM Agent，且包含 Challenge、Context Recovery、Dedup/Grouping，因此比规则扫描慢。

3. **人工审阅状态默认在本地浏览器**
   HTML 页面的勾选和备注默认保存在 localStorage。正式交接时必须导出 JSON，否则换电脑或换浏览器后状态不可见。

4. **功能边界需要权威证据**
   类似蓝牙问题说明：不能只凭风险文件中的一句话确认产品具备某功能，必须用设计输入、说明书、软件需求/设计、验证计划/报告等权威文件交叉确认。

5. **历史模块不应误当当前主线**
   `file_extractor/` 和 `schema/` 属于早期探索或关联资产，当前主线是 `m-agent/`。

## 9. 建议后续工作

### P0：先完成人工审阅闭环

1. 打开 `grouped-findings-review.html`。
2. 对 61 个问题组逐项人工判断：确认问题、误报、待补证或暂缓。
3. 导出 `human-review-decisions.json`。
4. 将确认误报或前提错误继续沉淀到 `report_corrections.yaml`。

### P1：降低重复运行成本

1. 新增“只基于已有 `grouping_results.json` 重新渲染 HTML”的轻量入口。
2. 避免每次调整报告样式都重新调用 Dedup/Grouping LLM。
3. 在日志中更明确显示当前卡在哪个 Agent、哪个 batch、哪一轮 Context Recovery。

### P2：增强结果质量评估

1. 用人工审阅结果建立 gold set。
2. 统计误报类型、漏报类型和高频错误前提。
3. 针对高频错误更新 Project Scout、Challenge、Context Recovery 和 Report Correction 规则。

### P3：适度拆分主脚本

当前主脚本功能集中，后续稳定后可以拆为：

```text
evidence_tools.py
agent_runtime.py
orchestrator.py
context_recovery.py
dedup_grouping.py
report_writer.py
cli.py
```

但建议在测试稳定后再拆，不要为拆而拆。


## 10. 关键路径索引

```text
m-agent/run_pt9l_full_audit_langgraph.py
m-agent/agent_configs/
magent-output/pt9l_full_audit_output_langgraph_v9/
magent-output/pt9l_full_audit_output_langgraph_v9/08_reports/grouped-findings-review.html
magent-output/pt9l_full_audit_output_langgraph_v9/15_dedup_grouping/grouping_results.json
magent-output/pt9l_full_audit_output_langgraph_v9/08_reports/run_summary_full_langgraph.json
magent-output/all_output.md
最新多智能体DHF-DMR流程架构可视化.html
PROJECT_HANDOVER.md
```
